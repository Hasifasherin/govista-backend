# GoVista Backend

Backend for **GoVista** built with **MERN stack**, **TypeScript**, **JWT authentication**, and **role-based access** (`user` / `operator`).

---

## Setup

1. Clone the repository:

```bash
git clone YOUR_REPO_URL
cd backend
